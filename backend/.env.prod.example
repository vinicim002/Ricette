# Copie para .env.prod (ou configure no provedor de deploy) e ajuste os valores.
# Nunca commite senhas reais no Git.

SPRING_PROFILES_ACTIVE=prod

SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/ricette
SPRING_DATASOURCE_USERNAME=ricette_user
SPRING_DATASOURCE_PASSWORD=troque-esta-senha

APP_ADMIN_EMAIL=admin@seudominio.com
APP_ADMIN_PASSWORD=troque-esta-senha

APP_JWT_SECRET=gere-um-segredo-longo-aleatorio-com-pelo-menos-32-caracteres
APP_JWT_EXPIRATION_MS=86400000

APP_CORS_ALLOWED_ORIGINS=https://ricette.seudominio.com
